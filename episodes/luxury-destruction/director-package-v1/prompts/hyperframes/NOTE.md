# prompts/hyperframes/ — empty, deliberately

`SPEC-Director-Worker-v1` §5 specifies this folder as part of every
Director Package's structure (image/keyframe prompts, per OS-009's
Higgsfield=video / HyperFrames=image split). It is empty for this episode
because nothing in the source Production Package's AI Generation Package
maps cleanly to "image generation" or "keyframe creation" as currently
scoped:

- The 5 generation blocks are all AI **video** (Higgsfield territory —
  see `../higgsfield/`).
- The reveal diagram ("The Markdown Tax") is a 2D animated build, not a
  static image or a video-camera shot.
- Real document screenshots (the two annual-report artifacts) and
  typography overlays aren't AI-generated at all.

This is recorded as production evidence, not resolved here — see
`../../production-log.md`.
