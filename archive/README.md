# archive/

## Purpose
Historical record only. Never loaded automatically by the runtime; used
only when explicitly requested.

## What belongs here
Superseded documents, deprecated artifacts, and anything kept for record
rather than active use.

## What must never be stored here
- Anything the runtime or a Worker needs to function
- Canonical documents still in force
- Active episode work

This folder is currently empty.
